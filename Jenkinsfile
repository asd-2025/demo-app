pipeline {
    agent any
    stages {
        stage('Test SSH') {
            steps {
                sh 'echo "Connexion SSH Jenkins-GitHub OK"'
                sh 'git --version'
            }
        }
    }
}
