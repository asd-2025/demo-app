pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo "Building the demo app..."
            }
        }

        stage('Test') {
            steps {
                echo "Running tests for demo app..."
            }
        }
    }
}
